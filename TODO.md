# Header Visibility Task

- [x] Update scroll handler in red-wasp/app/page.tsx to show header only when scrolled to top (scrollY === 0)
- [x] Remove unused lastScrollY ref from page.tsx
- [x] Test scrolling behavior to ensure header appears only at top
